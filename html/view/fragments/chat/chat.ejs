<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="utf-8">
    <title>Answer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link href="/chat.css" type="text/css" rel="stylesheet">
    <link href="./commonStyle.css" type="text/css" rel="stylesheet">
</head>
<body  style="height:100%;">
<div class="d-flex flex-column flex-shrink-0 bg-light">
    <div class="row">
        <div class="sidebar">
            <ul class="nav nav-pills nav-flush flex-column mb-auto text-center">
                <li class="nav-item side-button">
                    <button onclick="changeWindow('', 0);" href="#" class="nav-link active py-3 border-bottom" aria-current="page" title="" data-bs-toggle="tooltip" data-bs-placement="right" data-bs-original-title="Home">+</button>
                </li>
                <% for (let i = 0; i < friends.length; i++) { %>
                <% if (friends[i].realFriend) { %>
                <li class="nav-item side-button">
                    <button onclick="changeWindow('<%=friends[i].login%>', 1)" class="nav-link active py-3 border-bottom" aria-current="page" title="" data-bs-toggle="tooltip" data-bs-placement="right" data-bs-original-title="Home">
                    <%=friends[i].login%>
                    </button>
                </li>
                <% } %>
                <% } %>
            </ul>
        </div>
        <div class="col">
            <iframe id="window"></iframe>
        </div>
    </div>
</div>

<script>
    var iframe = document.getElementById('window')
    function changeWindow(login, type) {
        if (type == 0) {
            iframe.src = '/findFriendWindow'
        } else {
            iframe.src = '/subchat?index=no&friend=' + login
        }
    }
</script>

</body>